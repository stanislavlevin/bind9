By default, named is configured to run in readonly chroot jail with no
extra privileges.

To configure named as master or caching-only DNS, you don't need to create
files and/or directories writable by named.

To configure named as slave DNS in chroot jail, additional package
bind-slave have to be installed.

To debug named with rndc, additional package bind-debug have to be
installed.
